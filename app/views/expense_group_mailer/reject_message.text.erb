Hi <%= params[:user].email%>
You have applied for reimbursement named <%= params[:rejected] %> under the title <%= params[:title]%> for Rs.<%= params[:applied_amount]%>.
We regret to inform you that the expense reimbursement application for <%= params[:rejected]%> is rejected since it does not meet the criteria.
