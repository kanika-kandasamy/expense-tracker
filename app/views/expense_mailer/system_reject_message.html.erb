Hi <%= params[:user].email%>
You have applied for reimbursement named <%= params[:rejected]%> under <%= params[:type] %> for Rs.<%= params[:applied_amount]%>.
We regret to inform you that the expense reimbursement application is rejected since it does not meet the criteria.
